const validateAbout = require('./aboutValidation');
const validateProject = require('./projectValidation');
const validateSkill = require('./skillsValidation');
const validateStudies = require('./studiesValidation');
const validateWork = require('./workValidation');

module.exports = {
  validateAbout,
  validateProject,
  validateSkill,
  validateStudies,
  validateWork
}